# Cutover checklist — github-actions retirement

## Done in this session

- [x] Design + plan docs under `docs/superpowers/`
- [x] Automation drafts under `docs/superpowers/automations/`
- [x] `scripts/enable-bugbot-consumers.sh`
- [x] Confirmed `delete_branch_on_merge=true` on all six consumers
- [x] Removed `🔓 Review Gate` / `🤖 AI Review` from rulesets (ezzu, infra-template, lagn, aik)
- [x] Opened consumer strip PRs (links in README)
- [x] Interim local workflows for auto-assign + unmerged branch cleanup on those PRs

## Blocked — needs your Cursor API key / dashboard

Bugbot replied on infra-payload#41: **disabled for this repository**.

1. Create an API key at https://cursor.com/dashboard/api
2. Run:

```bash
export CURSOR_API_KEY=...
./scripts/enable-bugbot-consumers.sh
```

Or enable each repo in https://cursor.com/dashboard/bugbot

3. Prefer fail-on-unresolved in Bugbot org settings so findings are `failure` not `neutral`
4. After Bugbot posts checks, add **Cursor Bugbot** as a required status check on each consumer ruleset

## Manual UI (no public API)

For each consumer: Settings → General → Issues → ensure **Auto-close issues with merged linked pull requests** is **on** (especially eduly/ezzu, which had it off for `ai-qa`).

## After consumer PRs merge

1. Code-search: zero `EdulyCom/github-actions` callers on default branches
2. Archive this repo (`gh repo archive EdulyCom/github-actions -y`)
3. In Agents Window, create the two Automations from the drafts file; then delete interim workflows from consumers
4. Audit `ANTHROPIC_*` secrets before deletion (may still be used elsewhere)
